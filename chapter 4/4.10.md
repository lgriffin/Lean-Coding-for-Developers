## Error Handling Standard

Use AppError base class with specific error types.

Example: src/api/users/createUser.ts:45-52
Pattern: Fail fast at boundaries, throw with context

Don't: Return null/undefined, use generic Error, swallow errors silently

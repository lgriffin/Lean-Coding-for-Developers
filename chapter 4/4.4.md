Accept: GET /api/orders/export (authenticated user)
Return: CSV file with columns: order_id, date, total, status
Filter: Only orders belonging to the requesting user
Format: Standard CSV, UTF-8 encoding
Never: Include other users' data, load all orders into memory, allow unauthenticated access

# Find files modified most frequently in last 6 months
git log --since="6 months ago" --format=format: --name-only | \
  egrep -v '^$' | sort | uniq -c | sort -rn | head -10

# Example output:
#    87 src/services/order_processor.py     ← HIGH CHURN (modified in 60% of PRs)
#    62 src/models/payment.py               ← HIGH CHURN
#    41 src/api/checkout_handler.py         ← MEDIUM CHURN
#     8 src/reports/monthly_sales.py        ← LOW CHURN
#     2 src/reports/legacy_generator.py     ← LOW CHURN

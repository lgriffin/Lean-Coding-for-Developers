class InventoryManager {
  processStockTransfer(fromWarehouse: string, toWarehouse: string, sku: string, quantity: number) {
    // 200 lines of generated business logic
    // No unit tests
    // No integration tests you can run locally
    // No documentation of business rules
    // Changes require: edit → deploy to staging → manual test → find bug → repeat
    // Each iteration: 30 minutes
    // Decision inventory: dozens of rules, zero safety nets
  }
}
